kind = "web"
previewPath = "/"
title = "The Trio"
version = "1.0.0"
id = "artifacts/the-trio"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19576

[services.development]
run = "pnpm --filter @workspace/the-trio run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/the-trio", "run", "build" ]
publicDir = "artifacts/the-trio/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19576"
BASE_PATH = "/"
